import { ISprint } from '@/interfaces/ISprint';

export const staticLogicGenerateCombinations = (inputNamesInArray: string[]): ISprint[] => {
  console.log(inputNamesInArray);
  const sprints: ISprint[] = [];
  return sprints;
};
